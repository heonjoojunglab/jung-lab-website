# CLAUDE.md

이 파일은 Claude Code가 이 저장소에서 작업할 때 참고하는 안내 문서입니다.

## 프로젝트 개요

정헌주 교수(연세대학교 행정학과)의 개인 소개 홈페이지. 순수 HTML/CSS/JS 정적 사이트(빌드 도구·
프레임워크·백엔드 없음) — GitHub Pages, Netlify 등에 폴더째 올리면 바로 배포 가능.

- 언어: 한국어 기본, 영어 선택 가능(사이트 우측 상단 토글 버튼). 한 페이지(`index.html`) 안에
  한국어/영어 콘텐츠를 모두 넣어두고 CSS/JS로 전환하는 방식 — 별도 페이지·빌드 과정 없음.
- **2026-08-08부터 실제 배포됨**: `https://heonjoojunglab.github.io/jung-lab-website/`
  (GitHub Pages, `heonjoojunglab` 계정의 공개 저장소 `jung-lab-website`, `master` 브랜치
  루트에서 서빙). git 저장소이며 원격 `origin`이 연결돼 있음 — 이제 코드를 고치면
  **`git add . && git commit -m "..." && git push`까지 해야 실제 사이트에 반영됨**(로컬
  파일만 고치고 끝내면 배포판은 그대로임, 잊지 말 것).
  - **`assets/files/`는 `.gitignore`로 저장소에서 제외됨** — 이력서 원본 PDF에 개인 휴대폰
    번호가 들어있어서, 페이지에서 다운로드 버튼을 빼는 것만으로는 부족하다고 판단해(URL을
    알면 접근 가능하므로) 아예 git에 안 올리기로 함(2026-08-08). 실제 배포 사이트에서
    `assets/files/Heon_Joo_Jung_CV.pdf` 접근 시 404 나는 것 확인함. **새로 추가하는 파일에
    개인정보가 포함돼 있을 가능성이 있으면, 커밋 전에 항상 이 점을 먼저 확인할 것.**
  - GitHub CLI(`gh`)로 인증(`gh auth login --web`, 계정 `heonjoojunglab`)한 뒤 `gh repo
    create --public --source=. --push`로 저장소 생성+푸시, `gh api`로 Pages 활성화까지
    전부 이 세션에서 처음 설정함. 이 컴퓨터에 git/gh가 없어서 `winget`으로 설치부터 했음.

## 폴더 구성

```
Jung-lab-website/
├── index.html              페이지 전체(유일한 HTML 파일, 한/영 콘텐츠 모두 포함)
├── css/style.css           전체 스타일(네이비+골드 톤, 반응형)
├── js/main.js              언어 전환 로직 + 미디어 보도 자동 갱신 로직
└── assets/
    ├── images/
    │   ├── profile.jpg       프로필 사진(웹용, 정사각형 900×900 — 원본에서 얼굴 중심으로
    │   │                     크롭해 재생성함, 2026-08-08. 아래 "프로필 사진" 절 참고)
    │   └── profile-thumb.jpg 프로필 사진 작은 버전(정사각형 300×300, 현재 미사용 — 필요시 활용)
    └── files/
        └── Heon_Joo_Jung_CV.pdf   이력서 원본. **2026-08-08부터 페이지에서 링크가 빠짐**
                                    (사용자 요청 — "이력서 다운로드 링크를 없애줘"). 파일 자체는
                                    지우지 않고 남겨둠(나중에 다시 연결할 수도 있어서) — 그냥
                                    `index.html`에서 참조만 제거된 상태.
```

원본 프로필 사진과 이력서/소개글 PDF는 `C:\Users\Jungs\Cowork\Individual_info\`에 있음
(이 프로젝트 폴더 밖). 원본 사진은 4480×6720px, 13.7MB짜리라 그대로 쓰면 로딩이 느려서, PowerShell +
.NET `System.Drawing`으로 웹용 크기로 리사이즈해 `assets/images/`에 저장해 둠(원본은 건드리지 않음).

**프로필 사진 크롭 (2026-08-08)**: 처음엔 원본을 가로 900px로 리사이즈만 하고 CSS
`object-fit: cover`로 원형 크롭했더니, 인물이 사진 아래쪽에 앉은 자세라 원형 안에 얼굴보다
상반신이 더 많이 보였음 — 사용자가 "얼굴이 더 나오도록 재배치해달라"고 요청. CSS
`object-position`만 조정하는 대신, **원본에서 얼굴 중심의 정사각형 영역을 직접 잘라내
`profile.jpg`/`profile-thumb.jpg` 자체를 다시 생성**하는 방식을 택함(여러 크롭 후보를 만들어
직접 이미지로 확인해보고 제일 나은 것을 고름 — 정사각형 900×900이라 CSS 쪽은 그대로 `object-fit:
cover`만 있어도 전체가 딱 맞게 보임, 추가 조정 불필요). 원본 대비 크롭 좌표: 가로 900px 기준
`x=100, y=0, size=700`인 영역(가로폭 대비 얼굴이 살짝 왼쪽에 있어서 x를 안쪽으로 당김) —
이 비율을 원본 해상도(4480px)로 환산해 고화질로 다시 크롭함.

## 실행 / 미리보기

빌드 과정이 없어서 `index.html`을 브라우저로 바로 열어도 대부분 동작하지만, 미디어 보도 자동
갱신(아래 참고)이 `file://`에서 간헐적으로 막히는 브라우저가 있을 수 있어 로컬 서버로 띄우는 걸
권장:

```powershell
# 프로젝트 폴더에서
python -m http.server 8765
# 브라우저에서 http://localhost:8765 접속
```

## 콘텐츠 구조 (index.html 섹션 순서)

1. **헤더/내비게이션** — 브랜드 텍스트(`.brand`, 2026-08-08부터 "Heon Joo Jung's Lab"로 고정
   — 원래 "정헌주"/"Heon Joo Jung"를 언어별로 보여줬는데, 사용자 요청으로 언어 토글과 무관하게
   항상 이 영문 랩(연구실) 이름 하나로 통일함), 섹션 앵커 링크(#about #research #publications
   #media #contact), 언어 전환 버튼(`#lang-toggle`).
2. **히어로(소개)** — 사진 + 이름/직함 + 짧은 소개글(정헌주_short_bio.pdf 원문 그대로 사용) +
   연구분야 태그 + Google Scholar/연락처 버튼(**CV 다운로드 버튼은 2026-08-08에 뺌** — 위
   "폴더 구성"의 `assets/files/` 설명 참고).
3. **학력 및 경력** (`#cv`) — 학력·경력 타임라인(이력서 PDF 원문 기반), 현재 주요 보직, 학회 및
   대외 활동. 이력서의 "기획부학장/사회과학연구소장"(2023.03–2025.02)처럼 **이미 끝난 보직은
   "현재 주요 보직"에 넣지 않고 경력 타임라인에만 넣음** — short_bio.pdf가 더 최신 "현재 보직"
   목록이라 그쪽을 우선 신뢰함. 이력서에 새 보직이 추가되면 두 곳 다 확인해서 반영할 것.
4. **연구 프로젝트** (`#research`) — 2012~2025년 연구과제 24건을 표로 정리(이력서 원문 기반,
   연도 역순). 새 연구과제가 생기면 `<table class="project-table">`에 행 추가.
5. **논문 및 간행물** (`#publications`) — SSCI 5편을 상단에 하이라이트, 전체 75편(국내 학술지
   54편, 해외 학술지 16편, 저서(장) 2편, 정책보고서 3편)을 9개 주제 클러스터별 `<details>`
   (네이티브 접기/펼치기, JS 불필요)로 정리, **각 클러스터 안에서는 연도 역순(최신→과거)**으로
   정렬. 클러스터 하나가 원래 51편(2016년 이후 위주)이었다가, **2026-08-08에 연세대 교원정보
   페이지(`yupa.yonsei.ac.kr`, 정헌주 교수 개인 URL — CLAUDE.md에는 URL 미기재, 필요시 사용자에게
   재요청)의 공식 "논문(Journal Article)" 탭을 원자료로 검증해서 2005~2016년 논문 24편을 추가
   반영**함(당시 사이트는 이력서의 "최근 5년간 주요 저서 및 논문"만 반영해서 그 이전 논문이
   전부 빠져 있었음). 저자·권/호/페이지 등 서지사항은 Google Scholar 프로필
   (`scholar.google.com/citations?user=OIUiX-UAAAAJ`)의 논문별 상세정보로 보완 — 이 두 출처를
   교차 확인하는 방식이 표준. 원자료는 그 외에도
   `C:\Users\Jungs\Cowork\research\research_portfolio_summary.md`(영문 요약, 클러스터 분류
   포함). **논문 제목·서지사항은 언어 토글과 무관하게 원어 그대로 유지**(한국어 논문은 한글
   제목, 영어 논문은 영어 제목 — 학술 인용 관례상 번역하지 않음). 새 논문이 나오면 해당 주제
   클러스터의 `<ol>`에 **연도 역순 위치**에 항목 추가하고 `<span class="count">` 배지 숫자도
   같이 갱신 + 필요시 `<span class="badge-ssci">SSCI</span>` 배지 추가.
   - **저역서(책·편저)** (`#books`) — 논문 클러스터 아래에 별도 표로 정리(18건, 최신순). 연세대
     교원정보 페이지의 "저역서(Publications)" 탭이 원자료 — 이 탭은 책 제목·출판사·발행연월만
     주고 챕터 제목은 안 줘서, 책 전체 제목만 나열하는 방식. 이 중 2건("대전환기 정부혁신과
     공공정책 사례집 I", "한국 안보의 이해")은 위 논문 클러스터에도 정헌주 교수가 실제로 쓴
     장(chapter) 제목으로 개별 수록돼 있음 — 의도된 중복이며, 표 위에 안내 문구로 명시해 둠.
6. **미디어 보도** (`#media`) — 아래 "미디어 보도 자동 갱신" 절 참고. **2026-08-08에 블로그
   섹션(플레이스홀더)을 완전히 삭제**함(사용자 요청) — 헤딩도 "미디어 보도 & 블로그"에서
   "미디어 보도"로 변경, `css/style.css`의 `.blog-placeholder` 룰도 같이 제거. 블로그를 나중에
   시작하면 이 섹션을 다시 추가해야 함.
7. **연락처** (`#contact`) — 이메일, 연구실 전화, 연구실 주소(2026-08-08부터 **연희관 305호**로
   수정 — 이전엔 309-1호였음), Google Scholar. **개인 휴대폰 번호는 공개하지 않기로 확정**
   (사용자 확인받음, 2026-08-07) — 이력서 원본에는 있지만 사이트에는 의도적으로 뺐음. 나중에
   실수로 추가하지 않도록 주의.

## 다국어(한/영) 전환 방식

`css/style.css`의 `[data-lang="ko"] .lang-en { display:none }` / `[data-lang="en"] .lang-ko
{ display:none }` 규칙 + `js/main.js`가 `<html>`의 `data-lang` 속성을 버튼 클릭으로 전환
(localStorage에 저장해 다음 방문에도 유지, 기본값은 항상 "ko"). 새 텍스트를 추가할 때는 같은
자리에 `<span class="lang-ko">한국어</span><span class="lang-en">English</span>` 쌍으로 넣으면
자동으로 전환 대상이 됨 — 이 규칙을 안 지키면 언어를 바꿔도 그 텍스트만 계속 보이거나 안 보이게
됨. 문단처럼 긴 블록은 `<p class="hero-bio lang-ko">...</p>` 식으로 클래스만 추가하면 됨.

## 미디어 보도 자동 갱신 (핵심 기능)

**사용자 요청**: "미디어 보도는 인터넷을 통해서 자동으로 업데이트되도록 해줘." 순수 정적 사이트
조건 안에서 이걸 만족시키기 위해, 빌드/서버 없이 **브라우저가 페이지를 열 때마다 실시간으로 뉴스를
가져오는 클라이언트 사이드 방식**을 씀:

1. 구글 뉴스 RSS 검색: `https://news.google.com/rss/search?q=정헌주+연세대&hl=ko&gl=KR&ceid=KR:ko`
2. 이 RSS를 브라우저에서 직접 fetch하면 CORS에 막히므로, 무료 중계 서비스
   `api.rss2json.com`을 거쳐 JSON으로 받음(`Access-Control-Allow-Origin: *` 헤더가 있어
   브라우저 fetch가 실제로 통과되는 것을 PowerShell로 직접 검증함, 2026-08-07).
3. `js/main.js`의 `loadMediaFeed()`가 이 JSON을 받아 최근 8건을 `#media-feed` 목록에 렌더링.
   실패하면(오프라인, 서비스 장애 등) 구글 뉴스 검색 페이지로 바로 가는 링크를 대신 보여줌
   (`showFallback()`).
   - **2026-08-08부터 렌더링 전에 `pubDate` 기준 내림차순으로 직접 정렬**함(사용자 요청 —
     "시간 순서를 최근에서 오래된 순서로"). 구글 뉴스 RSS는 관련도순으로 응답이 뒤섞여 오는 걸
     실제로 확인했음(PowerShell로 직접 호출해서 날짜가 2026-07→2026-06→2025-05→2024-05 식으로
     뒤죽박죽인 것 검증, 2026-08-08) — `MAX_ITEMS`로 자르기 전에 정렬해야 최신 기사가 먼저
     보임. `rss2json`이 주는 `pubDate`는 `"YYYY-MM-DD HH:MM:SS"` 형식이라 `new Date()`로 바로
     파싱 가능.

**알아둘 점**:
- 검색어("정헌주 연세대")를 바꾸면 결과 범위가 달라짐 — `js/main.js` 맨 위 `SEARCH_QUERY`
  상수만 고치면 됨.
- rss2json.com은 무료 티어라 사용량 제한이 있을 수 있음(개인 홈페이지 트래픽 수준에서는 문제
  없을 것으로 예상, 확인은 못 함 — 이 프로젝트에 브라우저 자동화 도구가 없어 실제 대량 트래픽
  상황까지는 테스트 불가).
- 검색 결과 링크는 news.google.com의 리다이렉트 URL이라 원문 언론사 URL이 아님(구글 뉴스
  RSS의 기본 동작, 클릭하면 정상적으로 원문으로 넘어감).
- 동명이인 필터링은 검색어 좁히기 정도가 전부라 완벽하지 않음 — 관련 없는 기사가 섞이면
  `SEARCH_QUERY`를 더 구체적으로(예: 소속·직함 추가) 조정할 것.

## 검증 방법 (이 환경에 브라우저 자동화 도구 없음)

- HTML 구조: Python `html.parser`로 태그 짝 맞음 확인(`ast.parse` 같은 역할, 스크립트는 세션
  기록에만 있고 저장 안 해둠 — 필요하면 비슷한 스크립트를 다시 짜면 됨).
- 정적 자산: `python -m http.server`로 로컬 서빙 후 `Invoke-WebRequest`로 `index.html`,
  `css/style.css`, `js/main.js`, `assets/images/profile.jpg`, `assets/files/*.pdf` 전부
  200 응답 확인.
- 미디어 자동 갱신 로직: 실제 구글 뉴스 RSS + rss2json 프록시 조합을 PowerShell로 직접 호출해
  실제 관련 기사(연합뉴스·조선일보·파이낸셜뉴스 등)가 잡히는 것과 CORS 헤더가 열려 있는 것을
  확인.
- **실제 브라우저에서 언어 토글·반응형 레이아웃·미디어 목록 렌더링이 눈으로 봤을 때 정상인지는
  아직 미검증** — 이 환경에 브라우저 자동화 도구가 없어서. 사용자가 `http://localhost:8765`
  (또는 `index.html` 직접 열기)로 직접 확인 필요.

## 다음에 할 만한 일 (미착수)

- 블로그 링크 추가 (블로그가 생기면)
- 미디어 보도 자동 목록에 수동 큐레이션 옵션 추가 여부 검토(자동 목록에 정말 관련된 기사만
  나오는지 사용자가 몇 주 지켜본 뒤 결정하는 게 좋을 듯)
